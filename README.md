# tool-track
ToolTrack is a lightweight tool-management system using barcodes to track checkouts and returns. Built with C++/Qt and SQLite, it provides a simple way to record who borrowed each item and when it was returned.
